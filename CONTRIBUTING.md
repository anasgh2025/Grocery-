# Contributing

Quick steps to get started locally:

1. Ensure Flutter SDK is installed and `flutter doctor` passes.
2. Get packages: `flutter pub get`.
3. Run the app on an emulator or device: `flutter run`.

If you add fonts as files instead of using `google_fonts`, add them under `assets/fonts/` and update `pubspec.yaml` accordingly.
